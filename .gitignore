# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Environments
.env
.venv
.mapbox_token
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb
# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Project-specific
logs/
*.log

# Data files (keep structure, ignore content)
/notebooks/
/data/

# Data files (ignore structure and content)
*.csv
*.json
*.xlsx
*.parquet
*.db
# ...except hand-edited config, not generated data - must ship with the code
!config/client_services.json
# Shared with tds_faillure_code_agent (identical copy there): troubleshooting SSO config.
!config/troubleshooting_integration.json
# The declared column schema. Generated, but by `python -m src.campbell_ai.schema.build` and
# then committed on purpose: the service trusts it instead of reading 11 file headers on every
# session opening, so a schema change has to arrive as a reviewable diff and not as behaviour
# that shifted underneath. The vocabulary index is the opposite case and stays ignored - it
# lives under logs/ because it is derived from data that changes every half hour.
!src/campbell_ai/schema/dataset_columns.json
# Docker
*.pid
*.seed
*.pid.lock

# Temporary files
*.tmp
*.bak
*.swp

# Documentation claude
NAVIGATION_FIX_SUMMARY.md
IMPLEMENTATION_SUMMARY.md

dataDev/*
dataDev2/*
dataDep/*

new_dashboard/*
migration_dashboard/*
ai_docs/*

# PDF files (local reference docs)
*.pdf

# .pyc files
*.pyc